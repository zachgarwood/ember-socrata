import { module, test } from 'qunit';

module('Unit | Serializer | socrata', {
  // Specify the other units that are required for this test.
});

// Replace this with your real tests.
test('empty!', function(assert) {
  assert.ok(true);
});
